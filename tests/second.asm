;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 6161;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

add a1, 100;
;; simply increment rax to 50, rdx to 65530, a2 to 15, and halt before incrementing a1 to 100
inc rax;
inc rax;
add rax, rax;

add rax, 46;
sub rdx, 6; ;; 0 - 1 = 65535, 0 - 6 = 65530

inc a2;
inc a2;
dec a2;
dec a2;

nop;
nop;
nop;

add a2, 15;

nop;
nop;

;; time to halt!



;; will it halt? and not increment a1 to 100
;; it must execute 16 instructions  (not 17)

halt;

add a1, 100;
